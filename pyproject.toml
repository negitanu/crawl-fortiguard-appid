[project]
name = "crawl-fortiguard-appid"
version = "0.1.0"
description = "Crawler for FortiGuard App Control application IDs"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.31.0",
    "beautifulsoup4>=4.12.0",
    "lxml>=4.9.0",
    "rich>=13.0.0",
]
